name: resscan-env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.8
  - pip
  - bwa
  - diamond
  - samtools
  - pandas
  - numpy
  - nextflow >=23.0  # Added for the new resscan_batch orchestrator
  
  - pip:
    - .